database=mysql

spring.cloud.gcp.sql.database-name=mediator_db
#gcloud sql instances describe mediator-db | grep connectionName
spring.cloud.gcp.sql.instance-connection-name=feisty-lamp-316002:southamerica-east1:mediator-db

# Initialize the database since the newly created Cloud SQL database has no tables. The following flag is for Spring Boot 2.
spring.datasource.initialization-mode=always